#include <catch2/catch.hpp>
#include <histogram.h>
#include <gas_container.h>
#include <particle.h>

//using idealgas::GasContainer;
//GasContainer container = GasContainer(692, 108, 108, 692);
//
//using idealgas::Histogram;
//idealgas::Histogram histogram = Histogram(200, 75, 90, 250, "white", 10, 10);

TEST_CASE("Tests GetSpeedsOfParticlesList method") {
//  container.PopulateContainer("white", 10, 8, 10);
//  container.PopulateContainer("white", 15, 8, 20);
//  histogram.DrawHistogram(container.GetParticlesByMass(10));
//
//  std::vector<double> particles_speeds;
//
//  for (idealgas::Particle particle : container.GetParticlesByMass(10)) {
//    particles_speeds.push_back(particle.GetSpeed());
//  }
//  REQUIRE((std::equal(particles_speeds.begin(), particles_speeds.end(), container.GetParticlesByMass(10))) == true);
}
